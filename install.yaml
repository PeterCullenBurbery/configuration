install:
  programs to install:
    - Choco
    - Powershell 7
    - VS code
    - SQLite Browser
  logs:
    global log directory: |
      C:\logs
    per app log directories:
      cherry tree: |
        cherry-tree
  downloads:
    global download directory: |
      C:\downloads
    per app download directories:
      cherry tree: |
        cherry-tree